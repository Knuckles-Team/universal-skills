name: Cross Agent Integration Test Swarm
task_pattern: Perform Cross Agent Integration Test operations concurrently across all target layers
specialist_ids:
  - python-programmer
capability_overrides:
  python-programmer:
    - stuck_loop_detection
