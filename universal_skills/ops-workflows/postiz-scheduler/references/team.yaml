name: Postiz Scheduler Swarm
task_pattern: Perform Postiz Scheduler operations concurrently across all target layers
specialist_ids:
  - coordinator
  - project-manager
capability_overrides:
  coordinator:
    - stuck_loop_detection
  project-manager:
    - stuck_loop_detection
