{
  "mcpServers": {
    "repository-manager": {
      "command": "repository-manager-mcp",
      "args": [
        "--transport",
        "stdio"
      ],
      "env": {
        "FILE_OPERATIONSTOOL": "${FILE_OPERATIONSTOOL:-True}",
        "GIT_OPERATIONSTOOL": "${GIT_OPERATIONSTOOL:-True}",
        "MISCTOOL": "${MISCTOOL:-True}",
        "SYSTEM_OPERATIONSTOOL": "${SYSTEM_OPERATIONSTOOL:-True}"
      }
    }
  }
}
