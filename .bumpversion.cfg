[bumpversion]
current_version = 0.1.5
commit = True
tag = True

[bumpversion:file:pyproject.toml]
search = version = "{current_version}"
replace = version = "{new_version}"
search = universal-skills[agent-workflows]>="{current_version}"
replace = universal-skills[agent-workflows]>="{new_version}"
search = universal-skills[browser-tools]>="{current_version}"
replace = universal-skills[browser-tools]>="{new_version}"
search = universal-skills[cloudflare-deploy]>="{current_version}"
replace = universal-skills[cloudflare-deploy]>="{new_version}"
search = universal-skills[database-tools]>="{current_version}"
replace = universal-skills[database-tools]>="{new_version}"
search = universal-skills[developer-utilities]>="{current_version}"
replace = universal-skills[developer-utilities]>="{new_version}"
search = universal-skills[document-tools]>="{current_version}"
replace = universal-skills[document-tools]>="{new_version}"
search = universal-skills[github-tools]>="{current_version}"
replace = universal-skills[github-tools]>="{new_version}"
search = universal-skills[google-workspace]>="{current_version}"
replace = universal-skills[google-workspace]>="{new_version}"
search = universal-skills[jupyter-notebook]>="{current_version}"
replace = universal-skills[jupyter-notebook]>="{new_version}"
search = universal-skills[mcp-builder]>="{current_version}"
replace = universal-skills[mcp-builder]>="{new_version}"
search = universal-skills[project-planning]>="{current_version}"
replace = universal-skills[project-planning]>="{new_version}"
search = universal-skills[security-tools]>="{current_version}"
replace = universal-skills[security-tools]>="{new_version}"
search = universal-skills[skill-builder]>="{current_version}"
replace = universal-skills[skill-builder]>="{new_version}"
search = universal-skills[skill-graph-builder]>="{current_version}"
replace = universal-skills[skill-graph-builder]>="{new_version}"
search = universal-skills[system-tools]>="{current_version}"
replace = universal-skills[system-tools]>="{new_version}"
search = universal-skills[systems-manager]>="{current_version}"
replace = universal-skills[systems-manager]>="{new_version}"
search = universal-skills[web-artifacts]>="{current_version}"
replace = universal-skills[web-artifacts]>="{new_version}"
search = universal-skills[web-crawler]>="{current_version}"
replace = universal-skills[web-crawler]>="{new_version}"
search = universal-skills[web-searching]>="{current_version}"
replace = universal-skills[web-searching]>="{new_version}"
search = universal-skills[adguard-home-docs]>="{current_version}"
replace = universal-skills[adguard-home-docs]>="{new_version}"
search = universal-skills[git-cli-docs]>="{current_version}"
replace = universal-skills[git-cli-docs]>="{new_version}"
search = universal-skills[github-rest-api-docs]>="{current_version}"
replace = universal-skills[github-rest-api-docs]>="{new_version}"
search = universal-skills[gitlab-api-docs]>="{current_version}"
replace = universal-skills[gitlab-api-docs]>="{new_version}"
search = universal-skills[gofastmcp-docs]>="{current_version}"
replace = universal-skills[gofastmcp-docs]>="{new_version}"
search = universal-skills[jellyfin-docs]>="{current_version}"
replace = universal-skills[jellyfin-docs]>="{new_version}"
search = universal-skills[mealie-docs]>="{current_version}"
replace = universal-skills[mealie-docs]>="{new_version}"
search = universal-skills[microsoft-graph-api-docs]>="{current_version}"
replace = universal-skills[microsoft-graph-api-docs]>="{new_version}"
search = universal-skills[nextcloud-docs]>="{current_version}"
replace = universal-skills[nextcloud-docs]>="{new_version}"
search = universal-skills[pydantic-ai-docs]>="{current_version}"
replace = universal-skills[pydantic-ai-docs]>="{new_version}"
search = universal-skills[servicenow-api-docs]>="{current_version}"
replace = universal-skills[servicenow-api-docs]>="{new_version}"

[bumpversion:file:README.md]
search = Version: {current_version}
replace = Version: {new_version}

[bumpversion:file:universal_skills/skill_utilities.py]
search = __version__ = "{current_version}"
replace = __version__ = "{new_version}"
